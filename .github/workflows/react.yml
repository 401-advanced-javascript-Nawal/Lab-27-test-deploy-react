name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: echo Hello, world!
    - name: Run a multi-line script
      run: |
        git config --global user.name "${{ github.actor }}"
        git remote set-url origin https://${{ secrets.fb8044e5bf92d14f99bf18ffd20889dfe1a978f7 }}@github.com/${{ github.repository }}.git
        npm install
        npm run deploy
